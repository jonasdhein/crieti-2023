import { Text, View } from "react-native"
import { theme } from "../themes/Theme"

export const MapScreen = () => {
    return(
        <View style={theme.container}>
            <Text>MAPA AQUI</Text>
            {/* COLOQUEM O MAPA AQUI */}
        </View>
    )
}